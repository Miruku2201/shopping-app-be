#spring.application.name=shopping

server:
  port: 8080
  servlet:
    context-path: /shopping
spring:
  application:
    name: shopping
  datasource:
    url: 'jdbc:mysql://localhost:3306/shopping'
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true